# דוח סקירת קוד

| # | מיקום | בעיה | למה זה כפול/מיותר/מסורבל | שינוי מומלץ | סיכון | השפעה ובדיקות |
|---|---|---|---|---|---|---|
| 1 | `backend/mailRetrieving.py:17`, `code/Skeleton.py:11`, `code/gmail_download_attachment.py:12`, `code/Mails_Retrieving.py:6` | לוגיקת התחברות ל-Gmail API חוזרת בכמה וריאציות. | אותם imports, אותו scope, יצירת `Credentials`, `InstalledAppFlow`, ו-`build("gmail", "v1")` מופיעים בכמה קבצים עם הבדלים קטנים בנתיבי token/credentials. | לאחד למודול/מחלקה משותפים, למשל להשתמש ב-`MailRetrieving.connect()` או helper יחיד שמקבל נתיבים כפרמטרים. | בינונית | משפיע על כל סקריפטי Gmail. לבדוק OAuth refresh, התחברות ראשונה, ושימוש ב-token קיים. |
| 2 | `backend/mailRetrieving.py:92`, `code/gmail_download_attachment.py:33`, `code/gmail_download_attachment.py:47`, `code/Mails_Retrieving.py:9` | לוגיקת חיפוש הודעות והורדת attachments כפולה וחלקית. | `backend/mailRetrieving.py` כבר מכיל מימוש מקיף יותר: pagination, סינון קבצים, תיקייה לפי message id ושם קובץ בטוח. קבצי `code/*` נראים כמו גרסאות דמו/אב טיפוס. | להחליט אם `code/*` הם examples. אם כן, להפריד/לתעד. אם לא, להעביר אותם לשימוש במחלקה המרכזית במקום לשכפל. | בינונית | שינוי יכול להשפיע על הרצת סקריפטים ידנית. לבדוק הורדת קובץ יחיד, הורדה מרובת הודעות, וסינון PDF/images. |
| 3 | `code/Mails_Retrieving.py:3` | imports שאינם בשימוש: `base64`, `message_from_bytes`. | הקובץ רק מדפיס snippets, ולא מפענח raw email. | להסיר imports אם הקובץ נשאר כפי שהוא. | נמוכה | אין השפעה פונקציונלית צפויה. בדיקה: להריץ את הסקריפט או לפחות lint/static check. |
| 4 | `code/gmail_download_attachment.py:1`, `code/Skeleton.py:1` | `from __future__ import print_function` מיותר ב-Python 3. | הקוד משתמש ב-f-strings, ולכן ממילא דורש Python 3. | להסיר את ה-import. | נמוכה | אין השפעה ב-Python 3. בדיקה: `python -m py_compile` או הרצת הסקריפט. |
| 5 | `backend/mailRetrieving.py:171` | הערת "optional: keep your previous raw-email fetch" מצביעה על קוד legacy. | `get_emails()` לא נמצא בשימוש מחוץ לקובץ לפי חיפוש, וההערה עצמה מתארת קוד קודם ולא חוזה שימוש ברור. | לא למחוק מיד. או לתעד שזה API נתמך, או להעביר ל-example/test, או להסיר רק לאחר אישור שהוא לא נדרש. | בינונית | ייתכן שימוש ידני/עתידי. לבדוק חיפושים עקיפים והרצות ידניות לפני מחיקה. |
| 6 | `backend/package.json:13`, `backend/server.js:1` | תלויות backend לא בשימוש ישיר: `cors`, `dotenv`, `express`. | `server.js` משתמש רק ב-`http` המובנה של Node. אין `require("express")`, `require("cors")` או `require("dotenv")` בקוד. | לא להסיר בלי אישור. אם backend אמור להיות Express, להעביר את `server.js` ל-Express. אם לא, להסיר תלויות ולעדכן lockfile. | נמוכה-בינונית | הסרת תלויות משפיעה על התקנות ו-lockfile. בדיקות: `npm install`, הרצת server, בדיקת `/api/hello`. |
| 7 | `backend/package.json:4`, `backend/server.js:15` | `main` מצביע על `index.js`, אבל הקובץ בפועל הוא `server.js`; אין `start` script. | הרצת backend דרך npm אינה ברורה ועלולה להיכשל עבור כלים שמסתמכים על `main`. | לעדכן `main` ל-`server.js` ולהוסיף script כמו `start: node server.js`, אם זה entry point הרשמי. | נמוכה | משפר הרצה, לא משנה runtime אם מריצים `node server.js`. בדיקה: `npm start`, בדיקת endpoint. |
| 8 | `frontend/bills/src/App.jsx:1`, `frontend/bills/src/App.css:1`, `frontend/bills/README.md:1` | ה-frontend עדיין תבנית Vite/React ברירת מחדל. | `count`, לוגואים, טקסטי HMR ו-README template אינם קשורים לדומיין "Bills". זה לא dead code טכני, אבל כן קוד דמו שמחליף פונקציונליות אמיתית. | לאחר הגדרת דרישות UI, להחליף בקומפוננטת אפליקציה אמיתית ולהסיר assets/CSS של הדמו. | בינונית | ישנה את כל מסך הפתיחה. בדיקות: `npm run lint`, `npm run build`, בדיקה ידנית בדפדפן. |
| 9 | `frontend/bills/index.html:5`, `frontend/bills/public/vite.svg`, `frontend/bills/src/assets/react.svg` | assets של Vite/React משמשים רק לדמו. | אם מחליפים את מסך הדמו, הקבצים וה-favicon יהיו מיותרים. כרגע הם כן בשימוש ישיר, לכן לא מומלץ למחוק לפני שינוי UI. | להסיר רק כחלק מהחלפת הדמו. | נמוכה | משפיע על favicon ותצוגת הלוגואים. בדיקה: build ובדיקה שאין 404 assets. |
| 10 | `backend/mailRetrieving.py:92` | `download_all_receipt_attachments()` ארוכה ומערבבת אחריויות. | היא בונה query, מבצעת paging, מורידה metadata, מסננת סוגי קבצים, מורידה bytes, שומרת לקובץ ומדפיסה progress. | לפצל לפונקציות קטנות: איתור attachments, סינון filename, הורדת attachment, שמירה לדיסק. | בינונית | משפיע על flow המרכזי של הורדות. בדיקות עם mock ל-Gmail service, כולל הודעה בלי parts, קובץ ללא data, וקובץ עם שם לא תקין. |
| 11 | `backend/mailRetrieving.py:82` | מחרוזת query מסורבלת וקשה לתחזוקה. | תנאי subject רבים משולבים בתוך f-string אחד, כולל טקסט עברי שנראה מקודד לא נכון במסוף. | להוציא את מילות החיפוש לרשימה ולבנות query בצורה מובנית. לבדוק encoding לפני שינוי. | בינונית | שינוי קטן עלול לשנות תוצאות Gmail search. בדיקות: snapshot ל-query המתקבל עבור `None` ועבור `6m`. |
| 12 | `code/gmail_download_attachment.py:77` | שמירת attachment בשם המקורי בלי ניקוי שם קובץ. | ב-`backend/mailRetrieving.py` יש `_safe_filename()` שמטפל בתווים בעייתיים ב-Windows, אבל הסקריפט הזה לא משתמש בו. | להשתמש באותו helper משותף לניקוי שמות. | נמוכה | מפחית כשלי כתיבה/נתיבים לא תקינים. בדיקה עם filename שמכיל `\/:*?"<>|`. |

## הערות נוספות

יש קבצים לא מנוטרים/מקומיים כמו `backend/Cursor.lnk`, וגם `credentials.json`/`token.json` קיימים בדיסק אך מוגדרים ב-`.gitignore`, ולכן לא סומנו כקוד למחיקה.

בנוסף, `.idea` מופיע גם ב-git וגם ב-`.gitignore`; זה לא שינוי קוד, אבל כדאי להחליט בעתיד אם קבצי IDE צריכים להישאר מנוהלים.
